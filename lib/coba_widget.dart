import 'package:flutter/material.dart';

class CobaWidget extends StatelessWidget {
  const CobaWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
