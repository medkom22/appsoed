import 'package:flutter/material.dart';

class BiasaWidget extends StatelessWidget {
  const BiasaWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Biasa Widget"),
      ),
    );
  }
}
